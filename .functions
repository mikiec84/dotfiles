function nstart() {
    git init
    git add .
    git commit -m "initial"
    hub create Raynos/$(basename $PWD)
    npublish
    travisify
    travisify test
}

function ngenplus() {
    cd ~/projects
    ngen $1
    cd $1

    if [ ! -z $2 ]; then
        mv $2 ./index.js
    fi

    nstart
}
